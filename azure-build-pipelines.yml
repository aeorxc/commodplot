resources:
  repositories:
    - repository: pipeline-templates
      type: git
      name: Oil/pipeline-templates
      ref: refs/heads/diagnostic/uv-linux-lock-diff

extends:
  template: templates/python-packages-pipeline.yml@pipeline-templates
  parameters:
    packageName: 'commodplot'
    createRefinitivConfig: false
    dependencyMode: project
    dependencyGroups:
      - test

pool: Self-Hosted-AWS

trigger:
  branches:
    include:
      - main
  tags:
    include:
      - "*"
